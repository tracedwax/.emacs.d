;;; tdw-gtd-calendar.el --- Deterministic calendar sync -*- lexical-binding: t; -*-

;;; Commentary:
;; Stub - implementation lands in the GREEN step. See tdw-gtd-inbox.el's
;; commentary for why a bare stub (not a missing file) is needed for a
;; clean RED run in this test harness.

;;; Code:

(provide 'tdw-gtd-calendar)
;;; tdw-gtd-calendar.el ends here
