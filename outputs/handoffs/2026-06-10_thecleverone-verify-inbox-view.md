# Handoff prompt — verify Inbox View + Someday visibility fix (run on `thecleverone`)

Paste everything below the line into the Opus 4.6 agent running on `thecleverone`.

---

You are running on the machine `thecleverone`, which holds the **live org-gtd data** and the **running Emacs**. A second agent on another machine (`trace`) just made changes to the **shared `~/.emacs.d` git repo** (`github.com:tracedwax/.emacs.d`, branch `master`, HEAD should be `6ef37ef` or later) and needs you to verify them here against real data.

**Context — what changed and why:**
- A new **Inbox View** was added to `~/.emacs.d/config.org` (org is the source of truth; `config.el` is a gitignored tangle artifact regenerated by `(org-babel-load-file …)` in `init.el`).
- New functions: `tdw-inbox-view` (bound `C-c d i`, dispatch key `"i"`), `tdw/get-inbox-unestimated-count`, `tdw/agenda-move-to-inbox` (bound bare `i` in agenda), `tdw/format-view-banner`, `tdw/render-view-banner`.
- A prominent 2-line **banner** ("═══ 📥 INBOX VIEW ═══" + "Total Estimated Effort: H:MM") now renders on all 4 views (Inbox 📥 / Someday 💤 / Unordered 📋 / Ordered 🎯).
- **Critical fix to verify:** the Someday and Inbox views were changed from `tags-todo` to plain `tags`. Reason: `tags-todo` only matches headings with a TODO keyword, but org-gtd clears the keyword on Someday items (`org-todo ""`), so ~307 of ~332 Someday items were **invisible** in the Someday view. Plain `tags` surfaces them. No org data was ever modified or lost — this was purely a view-query limitation.

**Your job, in order. Do NOT modify any org data files. Report findings; don't "fix" anything beyond reloading config.**

### Phase 0 — Safety check FIRST (before pulling/reloading)
The user's org files are in git. Confirm nothing unexpected happened to the data:
1. For each org-gtd directory, run git status + diff. Primary one is `~/my-venndoor-life/orgnotes/gtd/`; also check any other life-area dirs that contain `org-gtd-tasks.org` (e.g. `~/my-bfc-life/`, `~/my-google-life/`, `~/my-bfc-partner-life/`, `~/my-personal-life/`).
   ```
   cd ~/my-venndoor-life && git --no-pager status && git --no-pager diff --stat
   ```
2. **Report:** Are there ANY uncommitted changes to `org-gtd-tasks.org` or other `.org` files? If yes, paste the diff stat — do NOT discard anything.

### Phase 1 — Update + reload `~/.emacs.d`
1. `cd ~/.emacs.d && git --no-pager status` — note if the working tree is clean. If there are local uncommitted changes, stash them first (`git stash`), pull, then report (don't drop the stash).
2. `git --no-pager pull --rebase origin master` (or `git --no-pager fetch && git --no-pager rebase origin/master`). Confirm HEAD is now `6ef37ef` or later: `git --no-pager log --oneline -3`.
3. Reload config in the running Emacs (this re-tangles + re-evals, applying the new `:bind` and defuns) by evaluating:
   ```elisp
   (org-babel-load-file "~/.emacs.d/config.org")
   ```
   (If you can't drive the live Emacs, tell the user to run `M-x org-babel-load-file RET ~/.emacs.d/config.org RET`, or restart Emacs.)

### Phase 2 — Confirm definitions + binding (resolves "C-c d i is undefined")
Evaluate and report each result:
```elisp
(fboundp 'tdw-inbox-view)             ; expect t
(fboundp 'tdw/agenda-move-to-inbox)   ; expect t
(fboundp 'tdw/format-view-banner)     ; expect t
(key-binding (kbd "C-c d i"))         ; expect tdw-inbox-view
```
If `tdw-inbox-view` is `t` but `C-c d i` still resolves to nil/undefined, evaluate `(use-package org-gtd …)` cannot be re-run easily — as a fallback eval `(global-set-key (kbd "C-c d i") #'tdw-inbox-view)` and report that this was needed.

### Phase 3 — Verify the Someday visibility fix (the important one)
Run this counting snippet and report the three numbers:
```elisp
(let ((all 0) (kwless 0) (donecncl 0))
  (dolist (f (org-agenda-files))
    (when (file-exists-p f)
      (with-current-buffer (find-file-noselect f)
        (org-with-wide-buffer
         (goto-char (point-min))
         (org-map-entries
          (lambda ()
            (when (string= (org-entry-get (point) "ORG_GTD") "Someday")
              (cl-incf all)
              (let ((td (org-get-todo-state)))
                (cond ((null td) (cl-incf kwless))
                      ((member td '("DONE" "CNCL")) (cl-incf donecncl))))))
          nil nil)))))
  (message "Someday total=%d  keyword-less=%d  done/cncl=%d" all kwless donecncl))
```
Then open the Someday view: `M-x tdw-someday-priority-view` (or `C-c d S`). **Report:** roughly how many items now show vs. before (should be the full set now, ~`all`), and whether the 📥/💤 banner appears at the top.

### Phase 4 — DONE/CNCL exclusion experiment (decides a pending design choice)
The fix currently uses plain `tags`, so DONE/CNCL Someday items now also appear. We want to hide those **without** re-hiding the keyword-less ones. Determine the correct match syntax by reporting the count from each of these (they use the same matcher the agenda uses):
```elisp
(length (org-map-entries t "LEVEL>0+ORG_GTD=\"Someday\"" 'agenda))                 ; A: all
(length (org-map-entries t "LEVEL>0+ORG_GTD=\"Someday\"/-DONE-CNCL" 'agenda))      ; B: slash-negation
(length (org-map-entries t "LEVEL>0+ORG_GTD=\"Someday\"-TODO=\"DONE\"-TODO=\"CNCL\"" 'agenda)) ; C: inline
```
**Report A, B, C.** Interpretation (for the `trace` agent): if B (or C) equals `all - done/cncl` and is much larger than ~25, that syntax keeps keyword-less items and we'll adopt it; if B/C collapse to ~25, that syntax wrongly drops keyword-less items and we keep plain `tags`.

### Phase 5 — Functional smoke test (safe; creates one throwaway item)
1. Capture a test item: `C-c d c i`, title "ZZ inbox test", finish capture. Confirm the raw heading in `~/my-venndoor-life/orgnotes/gtd/inbox.org` looks like:
   ```
   * TODO ZZ inbox test
   :PROPERTIES:
   :ORG_GTD:  Inbox
   ...
   :END:
   ```
2. Open `C-c d i` (Inbox View) — confirm "ZZ inbox test" appears (likely under Unestimated).
3. On that item press `n` (→ Next Action), confirm it leaves the inbox and lands in Actions. Then in an Actions view press `i` on it — confirm it returns to the Inbox view (re-tagged `ORG_GTD=Inbox`). Then press `x` to cancel it / delete the test heading so no litter remains.
4. **Report** whether `s` / `n` / `x` / `i` all behaved.

### Report back (answer all):
1. Phase 0: any unexpected org-file git changes? (yes/no + diff stat)
2. Phase 2: the four eval results; did `C-c d i` need the fallback?
3. Phase 3: `total / keyword-less / done-cncl` counts; do all Someday items show now? banner present?
4. Phase 4: counts A, B, C.
5. Phase 5: did capture + s/n/x/i work? any errors (paste backtraces)?
6. Anything surprising or broken.
